
# #######################################################################################
# AUTOGENERATED HYPR CONFIG.
# PLEASE USE THE CONFIG PROVIDED IN THE GIT REPO /examples/hypr.conf AND EDIT IT,
# OR EDIT THIS ONE ACCORDING TO THE WIKI INSTRUCTIONS.
# #######################################################################################

# autogenerated = 1 # remove this line to remove the warning

# This is an example Hyprland config file.
# Refer to the wiki for more information.
# https://wiki.hyprland.org/Configuring/

# Please note not all available settings / options are set here.
# For a full list, see the wiki

# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:
# source = ~/.config/hypr/myColors.conf

# For community recommendations on apps and utilities, see
# https://github.com/hyprland-community/awesome-hyprland


################
### MONITORS ###
################

# FIXME: this doesn't work. manually run the lid open script
#monitor = , preferred, auto, 
#monitor = eDP-1, preferred, auto-down, 1  # always place laptop screen below external monitors

# Enable clamshell mode: turn off laptop monitor when lid is closed
#bindl = , switch:on:Lid Switch, exec, /home/graham/.utils/hypr_lid.sh close
#bindl = , switch:off:Lid Switch, exec, /home/graham/.utils/hypr_lid.sh open

# Monitor hotplug detection
#bind = , DEVICE_ADDED, exec, /home/graham/.utils/hypr_lid.sh check
#bind = , DEVICE_REMOVED, exec, /home/graham/.utils/hypr_lid.sh check


###################
### MY PROGRAMS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/

# Set programs that you use
$terminal = kitty
$terminal_trusted = kitty -o clipboard_control="write-clipboard write-primary read-clipboard read-primary no-ask"
$fileManager = dolphin
$menu = wofi --show drun
$dropterm = $HOME/.utils/quake-term.sh
$dropperp = $HOME/.utils/quake-perp.sh
$dropnote = $HOME/.utils/quake-note.sh
$dropbook = $HOME/.utils/quake-book.sh
$dropclip = $HOME/.utils/quake-clip.sh
$browser = firefox-bin
$screenshot = sh -c 'file=/home/graham/Downloads/screenshots/$(date +%Y-%m-%d_%H-%M-%S).png; slurp | grim -g - "$file" && wl-copy < "$file"'


#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

exec-once = $terminal
exec-once = /home/graham/.utils/tmux-at-startup.sh
# need a firefox instance open for quake-perp to work right
# exec-once = hyprctl dispatch exec "[workspace 1 silent] firefox-bin"
# exec-once = nm-applet &
# exec-once = waybar & hyprpaper & firefox
exec-once = hyprpaper
# watch for clipboard changes and record with 'cliphist'
exec-once = wl-paste --type text --watch cliphist store


#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hyprland.org/Configuring/Environment-variables/

env = XCURSOR_THEME,Adwaita
env = XCURSOR_SIZE,24
env = HYPRCURSOR_THEME,Adwaita
env = HYPRCURSOR_SIZE,24
exec-once = hyprctl setcursor Adwaita 24

# set new mouse cursors for gtk/gnome applications
exec-once = gsettings set org.gnome.desktop.interface cursor-theme 'Adwaita'
exec-once = gsettings set org.gnome.desktop.interface cursor-size 24

#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general {
    gaps_in = 9
    gaps_out = 12

    border_size = 2

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    col.inactive_border = rgba(595959aa)

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = false

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = dwindle
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 5

    # Change transparency of focused and unfocused windows
    active_opacity = 0.90
    inactive_opacity = 0.90
    fullscreen_opacity = 0.90

    shadow {
        enabled = true
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 4
        passes = 1
        #vibrancy = 0.2
        #vibrancy_darkness = 0.1
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = yes, please :)

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1

    animation = global, 1, 10, default
    animation = border, 1, 5.39, easeOutQuint
    animation = windows, 1, 4.79, easeOutQuint
    animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
    animation = windowsOut, 1, 1.49, linear, popin 87%
    animation = fadeIn, 1, 1.73, almostLinear
    animation = fadeOut, 1, 1.46, almostLinear
    animation = fade, 1, 3.03, quick
    animation = layers, 1, 3.81, easeOutQuint
    animation = layersIn, 1, 4, easeOutQuint, fade
    animation = layersOut, 1, 1.5, linear, fade
    animation = fadeLayersIn, 1, 1.79, almostLinear
    animation = fadeLayersOut, 1, 1.39, almostLinear
    animation = workspaces, 1, 1.94, almostLinear, fade
    animation = workspacesIn, 1, 1.21, almostLinear, fade
    animation = workspacesOut, 1, 1.94, almostLinear, fade
}

# Ref https://wiki.hyprland.org/Configuring/Workspace-Rules/
# "Smart gaps" / "No gaps when only"
# uncomment all if you wish to use that.
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrulev2 = bordersize 0, floating:0, onworkspace:w[tv1]
# windowrulev2 = rounding 0, floating:0, onworkspace:w[tv1]
# windowrulev2 = bordersize 0, floating:0, onworkspace:f[1]
# windowrulev2 = rounding 0, floating:0, onworkspace:f[1]

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}


#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
# alternative triggers
# grp:win_space_toggle → Super (Win) + Space
# grp:alt_space_toggle → Alt + Space
# grp:shifts_toggle → both Shift keys together
# grp:alts_toggle → both Alt keys together
# grp:ctrls_toggle → both Ctrl keys together
# grp:caps_toggle → Caps Lock
# grp:shift_caps_toggle → Shift + Caps Lock
# grp:alt_caps_toggle → Alt + Caps Lock
# grp:lwin_toggle / grp:rwin_toggle → left or right Super key
input {
    kb_layout = us,gr
    kb_variant = ,polytonic
    kb_model =
    kb_options = ctrl:nocaps
    kb_options = grp:win_space_toggle
    kb_rules =

    follow_mouse = 1

    sensitivity = 0.5 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = false
    }

    repeat_delay = 500   # try 500–700 instead of the default ~300
    repeat_rate  = 30    # slower repeats (defaults are often 30–40)
}

# https://wiki.hyprland.org/Configuring/Variables/#gestures

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
    name = logitech-m510
    sensitivity = 0.3
}


###################
### KEYBINDINGS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

bind = ALT, tab, cyclenext

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, X, exec, $terminal
bind = $mainMod SHIFT, X, exec, $terminal_trusted
bind = $mainMod, B, exec, $browser
bind = $mainMod, Q, killactive,
bind = $mainMod, F, fullscreen
bind = $mainMod, M, exit,
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, V, togglefloating,
bind = $mainMod, T, setprop, active opaque toggle
bind = $mainMod, R, exec, $menu
bind = $mainMod, P, pseudo, # dwindle
#bind = $mainMod, J, togglesplit, # dwindle

bind = Control_L, 0, exec, $dropclip
bind = Control_L, 9, exec, $dropterm
bind = Control_L, 8, exec, $dropbook
bind = Control_L, 7, exec, $dropperp
# this may need to be customized per machine
bind = , Print, exec, $screenshot

# Move focus with mainMod + arrow keys
# bind = $mainMod, left, movefocus, l
# bind = $mainMod, right, movefocus, r
# bind = $mainMod, up, movefocus, u
# bind = $mainMod, down, movefocus, d
bind = $mainMod, h, movefocus, l
bind = $mainMod, l, movefocus, r
bind = $mainMod, k, movefocus, u
bind = $mainMod, j, movefocus, d

bind = $mainMod SHIFT, h, movewindow, l
bind = $mainMod SHIFT, l, movewindow, r
bind = $mainMod SHIFT, k, movewindow, u
bind = $mainMod SHIFT, j, movewindow, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl s 10%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl s 10%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# Example windowrule v1
# windowrule = float, ^(kitty)$

# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$

## Deprecated in Hyprland 0.53
#
# windowrulev2 = opacity 1.0 override 1.0 override,class:^(kitty)$
# windowrulev2 = opacity 1.0 override 1.0 override,class:^(dropdown)$
# windowrulev2 = float,class:^(dropdown)$
# windowrulev2 = stayfocused,class:^(dropdown)$
#
# # Ignore maximize requests from apps. You'll probably like this.
# windowrulev2 = suppressevent maximize, class:.*
#
# # Fix some dragging issues with XWayland
# windowrulev2 = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0

# --- dropdown / kitty opacity & focus ---
# Kitty and dropdown always fully opaque
windowrule = opacity 1.0 override 1.0 override, match:class ^(kitty)$
windowrule = opacity 1.0 override 1.0 override, match:class ^(dropdown)$
# The above rules don't override fullscreen opacity, so kitty falls back to
# the global fullscreen_opacity (0.90) when fullscreened. To fix, uncomment:
#windowrule = opacity 1.0 override 1.0 override 1.0 override, match:class ^(kitty)$
#windowrule = opacity 1.0 override 1.0 override 1.0 override, match:class ^(dropdown)$

# Dropdown windows float and stay focused (pure new syntax)
windowrule = float on, match:class ^(dropdown)$
windowrule = stay_focused on, match:class ^(dropdown)$

windowrule = suppress_event maximize, match:class .*

# Xwayland helper: do not focus initially
windowrule = no_initial_focus on, match:class ^$, match:title ^$, match:xwayland true


###############################
### HOST SPECIFIC OVERRIDES ###
###############################

# See https://wiki.hyprland.org/Configuring/Monitors/
exec-once = ln -sf "./hypr-$HOSTNAME.conf" /home/graham/.config/hypr/hostname-specific.conf
source = /home/graham/.config/hypr/hostname-specific.conf
